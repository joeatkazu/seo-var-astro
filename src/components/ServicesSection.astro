---
import { Search, Brain, Globe, ArrowRight } from "lucide-react";

const accentColors = [
  { bg: "bg-blue-50", text: "text-blue-600", hover: "hover:text-blue-700" },
  { bg: "bg-indigo-50", text: "text-indigo-600", hover: "hover:text-indigo-700" },
  { bg: "bg-teal-50", text: "text-teal-600", hover: "hover:text-teal-700" },
];

const services = [
  {
    icon: Search,
    slug: "seo-strategia",
    title: "SEO stratégia és kivitelezés",
    description: "A technikai alapozás, a stratégiai tartalomgyártás és a prémium linképítés nálunk egymást erősítő, egységes rendszert alkot. Nem elszigetelt feladatokat végzünk, hanem az ön üzleti céljaihoz igazított folyamatokat építünk ki, amelyek kiszámítható piaci előnyt és mérhető bevételnövekedést eredményeznek.",
  },
  {
    icon: Brain,
    slug: "ai-kereses",
    title: "AI-optimalizálás és láthatóság (GEO)",
    description: "Márkapozicionálás a ChatGPT, a Gemini és a Google AI Overviews felületein. Stratégiánk biztosítja, hogy az ön cége elsődleges forrásként és hiteles válaszként jelenjen meg, amikor potenciális ügyfelei az intelligens rendszerektől kérdeznek.",
  },
  {
    icon: Globe,
    slug: "helyi-seo",
    title: "Helyi SEO és Google Térkép optimalizálás",
    description: "Domináns jelenlét a Local Pack pozíciókban és célzott lokális láthatóság a hazai piaci szereplők számára. Biztosítjuk a GBP (Google Business Profile) entitás technikai integritását és a folyamatos geolokációs marker optimalizálást, ami közvetlen növekedést eredményez a telefonhívások, az ajánlatkérések és a térségi piaci részesedés terén.",
  },
];
---

<section id="szolgaltatasok" class="py-20">
  <div class="container mx-auto px-4">
    <div class="mx-auto mb-16 max-w-3xl text-center">
      <span class="text-sm font-semibold uppercase tracking-wider text-[hsl(var(--primary))]">Szolgáltatásaink</span>
      <h2 class="mt-3 text-3xl font-bold text-slate-900 sm:text-4xl">
        Komplex láthatósági stratégia: a keresőoptimalizálástól az AI-integrációig
      </h2>
      <p class="mt-4 text-lg text-slate-600">
        Többszintű digitális jelenlét, amely a precíz technikai alapozást és a releváns tartalomstratégiát ötvözi az ön piaci előnyének maximalizálásáért.
      </p>
    </div>

    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
      {services.map((service, index) => {
        const accent = accentColors[index];
        return (
          <div
            class="group rounded-2xl border border-slate-100 bg-white p-8 shadow-sm transition-all duration-300 hover:shadow-xl"
          >
            <div class={`mb-6 flex h-14 w-14 items-center justify-center rounded-xl ${accent.bg} ${accent.text} transition-transform duration-300 group-hover:scale-110`}>
              <service.icon size={28} />
            </div>
            <h3 class="mb-3 text-xl font-bold text-slate-900">{service.title}</h3>
            <p class="mb-6 text-sm leading-relaxed text-slate-600">
              {service.description}
            </p>
            <a href={`/szolgaltatasok/${service.slug}`} class={`inline-flex items-center text-sm font-semibold ${accent.text} ${accent.hover}`}>
              Tovább
              <ArrowRight size={16} class="ml-1 transition-transform group-hover:translate-x-1" />
            </a>
          </div>
        );
      })}
    </div>
  </div>
</section>
