---
import { ArrowRight } from "lucide-react";
---

<section class="py-20">
  <div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
    <div class="relative overflow-hidden rounded-3xl bg-slate-900 p-10 text-center shadow-2xl md:p-16">
      <!-- Decorative blur circles -->
      <div class="absolute -mr-20 -mt-20 right-0 top-0 h-80 w-80 rounded-full bg-blue-600 opacity-20 blur-3xl"></div>
      <div class="absolute -mb-20 -ml-20 bottom-0 left-0 h-80 w-80 rounded-full bg-purple-600 opacity-20 blur-3xl"></div>

      <h2 class="relative z-10 mb-6 text-3xl font-bold text-white sm:text-4xl">
        Készen állsz a növekedésre?
      </h2>
      <p class="relative z-10 mx-auto mb-10 max-w-2xl text-lg text-slate-300">
        Foglaljon időpontot egy ingyenes konzultációra, és megmutatjuk, hogyan teheti márkáját megkerülhetetlenné a keresők és az AI világában! 
      </p>
      <a
        href="/#kapcsolat"
        class="relative z-10 inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-xl bg-[hsl(var(--primary))] px-8 py-4 text-base font-bold text-white shadow-lg transition-all hover:bg-blue-500 hover:shadow-blue-500/50"
      >
        Ingyenes konzultáció
        <ArrowRight size={18} />
      </a>
    </div>
  </div>
</section>
