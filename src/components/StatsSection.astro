---
const stats = [
  { value: "150+", label: "Sikeres projekt" },
  { value: "3x", label: "Atlagos forgalomnovekedés" },
  { value: "92%", label: "Ugyfel-elegedettseg" },
  { value: "Top 3", label: "AI keresesi pozicio" },
];
---

<section id="eredmenyek" class="border-y border-[hsl(var(--border))]/50 bg-[hsl(var(--card))] py-20">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-2 gap-8 lg:grid-cols-4">
      {stats.map((stat) => (
        <div class="text-center">
          <div class="mb-2 font-[var(--font-display)] text-4xl font-bold gradient-text text-glow sm:text-5xl">
            {stat.value}
          </div>
          <div class="text-sm text-[hsl(var(--muted-foreground))]">{stat.label}</div>
        </div>
      ))}
    </div>
  </div>
</section>
