---
const stats = [
  { value: "150+", label: "Sikeres projekt" },
  { value: "3x", label: "Atlagos forgalomnovekedés" },
  { value: "92%", label: "Ugyfel-elegedettseg" },
  { value: "Top 3", label: "AI keresesi pozicio" },
];
---

<section id="eredmenyek" class="bg-[hsl(var(--primary))] py-16 text-white">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-2 gap-8 text-center divide-x divide-white/20 md:grid-cols-4">
      {stats.map((stat) => (
        <div class="p-4">
          <div class="mb-2 text-4xl font-bold lg:text-5xl">
            {stat.value}
          </div>
          <div class="text-sm font-medium uppercase tracking-wide text-blue-100">{stat.label}</div>
        </div>
      ))}
    </div>
  </div>
</section>
