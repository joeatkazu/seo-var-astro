---
import { ArrowRight, Sparkles } from "lucide-react";
---

<section class="relative overflow-hidden pt-32 pb-20 lg:pt-40 lg:pb-28">
  <!-- Decorative gradients -->
  <div class="absolute top-0 right-0 -z-10 h-full w-1/2 bg-gradient-to-bl from-blue-50 to-transparent"></div>
  <div class="absolute bottom-0 left-0 -z-10 h-1/2 w-full bg-gradient-to-t from-slate-100/50 to-transparent"></div>

  <div class="container relative mx-auto px-4">
    <div class="grid items-center gap-12 lg:grid-cols-2 lg:gap-4">
      <!-- Left column: text -->
      <div class="lg:pl-12 lg:pr-4">
        <div class="mb-6 inline-flex items-center gap-2 rounded-full border border-blue-100 bg-blue-50 px-3 py-1 text-sm font-semibold text-[hsl(var(--primary))] animate-[fade-in-up_0.8s_ease-out_forwards]">
          <span class="relative flex h-2 w-2">
            <span class="absolute inline-flex h-full w-full animate-ping rounded-full bg-blue-400 opacity-75"></span>
            <span class="relative inline-flex h-2 w-2 rounded-full bg-blue-500"></span>
          </span>
          AI-alapú keresőoptimalizálás
        </div>

        <h1 class="mb-6 text-4xl font-bold leading-[1.15] text-slate-900 sm:text-5xl lg:text-[3.5rem] animate-[fade-in-up_0.8s_ease-out_0.1s_forwards] opacity-0">
          Budapesti{" "}
          <span class="text-[hsl(var(--primary))]">SEO és AI</span>
          <br />
          keresőoptimalizálás.
        </h1>

        <p class="mb-8 max-w-lg text-lg leading-relaxed text-slate-600 animate-[fade-in-up_0.8s_ease-out_0.2s_forwards] opacity-0">
          Szakmai támogatást nyújtunk a hazai vállalatok piacvezetővé válásához a digitális térben. Felkészítjük üzleti folyamatait az AI-alapú keresési ökoszisztémára: komplex keresőoptimalizálási stratégiával biztosítjuk, hogy márkája elsődleges hivatkozási ponttá váljon a hagyományos találati listákon, a Google AI Overviews összefoglalóiban, valamint a vezető AI-platformok (ChatGPT, Gemini) adatforrásaiban.
        </p>

        <div class="flex flex-col gap-4 sm:flex-row animate-[fade-in-up_0.8s_ease-out_0.3s_forwards] opacity-0">
          <a
            href="/#kapcsolat"
            class="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-xl bg-[hsl(var(--primary))] px-8 py-4 text-base font-semibold text-white shadow-xl shadow-blue-200 transition-all hover:bg-[hsl(var(--primary))]/90 hover:shadow-blue-300 hover:-translate-y-1"
          >
            Konzultáció
            <ArrowRight size={18} />
          </a>
          <a
            href="/#szolgaltatasok"
            class="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-xl border border-slate-200 bg-white px-8 py-4 text-base font-semibold text-slate-700 shadow-sm transition-all hover:border-slate-300 hover:bg-slate-50"
          >
            Szolgáltatásaink
          </a>
        </div>

        <!-- Trusted by -->
        <div class="mt-10 flex items-center gap-4 text-sm text-slate-500 animate-[fade-in-up_0.8s_ease-out_0.4s_forwards] opacity-0">
        
        </div>
      </div>

      <!-- Right column: hero image -->
      <div class="relative flex w-full items-center justify-center lg:h-[600px]">
        <div class="absolute inset-0 translate-x-12 translate-y-12 rounded-full bg-blue-100/50 blur-3xl"></div>
        <img
          src="/hero.png"
          alt="KeresoPartner SEO és AI keresőoptimalizálás"
          class="relative z-10 w-full max-w-lg rounded-2xl shadow-2xl shadow-slate-200/50"
          loading="eager"
        />
      </div>
    </div>
  </div>
</section>
